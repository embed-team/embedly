{
  "name": "@embedly/instagram-fetcher",
  "version": "0.15.3",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "wrangler dev --env us-west",
    "deploy:us-west": "wrangler deploy --env us-west --minify",
    "deploy:us-east": "wrangler deploy --env us-east --minify",
    "deploy:eu-west": "wrangler deploy --env eu-west --minify",
    "cf-typegen": "wrangler types --env us-west --env-interface CloudflareBindings"
  },
  "devDependencies": {
    "@embedly/config": "workspace:*",
    "@types/node": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "wrangler": "^4.114.0"
  }
}
