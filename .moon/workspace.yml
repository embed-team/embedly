projects: ["packages/*", "apps/*"]

vcs:
  client: "git"
  defaultBranch: "rewrite"
  provider: "github"
  sync: true
  hooks:
    pre-commit:
      - "moon run :lint :fmt/check --affected --status=staged"
    commit-msg:
      - "pnpm commitlint --edit $ARG1"
