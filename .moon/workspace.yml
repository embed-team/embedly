projects: ["packages/*", "apps/*"]

vcs:
  client: "git"
  defaultBranch: "rewrite"
  provider: "github"
  sync: true
  hooks:
    pre-commit:
      - "moon run :lint :format --affected --status=staged"
    commit-msg:
      - "pnpm commitlint --edit $ARG1"
